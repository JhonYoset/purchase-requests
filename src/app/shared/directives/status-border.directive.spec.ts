import { StatusBorderDirective } from './status-border.directive';

describe('StatusBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusBorderDirective();
    expect(directive).toBeTruthy();
  });
});
